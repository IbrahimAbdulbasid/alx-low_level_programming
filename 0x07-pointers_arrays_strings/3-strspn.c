#include "main.h"
/**
 * _strspn - entery level
 * @s: input
 * @accept: input
 * Return: always 0
*/
unsigned int _strspn(char *s, char *accept)
{
	char *s = Welcome;

	char *accept = Ibrahim;

	unsigned int r;

	r = _strspn(accept, r);

	_putchar("%u\n", r);

	return (0);
}
